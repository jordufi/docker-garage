# Home Server Orchestrator Configuration
CONTROLLER_PORT=8081
UI_PORT=8080
APPS_DIR=./apps

# Your machine's LAN IP address.
# Used to generate app URLs accessible from other devices (e.g. phone).
# Find it with: ipconfig (Windows) or ip a (Linux)
# Example: 192.168.1.40
HOST_IP=localhost

# Host-side absolute path to the apps folder.
# Managed apps use ${HOST_APPS_DIR} for bind mounts.
# Set this to the full path on your host machine.
# On Linux/macOS:  /home/user/homeserver/apps
# On Windows WSL:  /mnt/c/Users/user/homeserver/apps
# Leave empty to auto-detect (works when APPS_DIR is relative).
HOST_APPS_DIR=
